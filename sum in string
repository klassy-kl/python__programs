def findsum(string):
    a='0'
    sum=0
    for i in string:
        if i.isdigit():
            a=a+i 
        else:
            sum=sum+int(a)
            a='0'
    return sum+int(a)  
string='12gayn34h5'    #51
print(findsum(string))
